import React from 'react';
import { Container, Typography } from '@mui/material';

const Offers = () => {
  return (
    <Container>
      <Typography variant="h4">Offers</Typography>
      {/* Add content for loan offers */}
    </Container>
  );
};

export default Offers;
